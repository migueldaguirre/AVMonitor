wb = xlsx_package.workbook

wb.add_worksheet(name: "Registros") do |sheet|
  # this is the head row of your spreadsheet
  sheet.add_row %w(PRODUCTO NIVEL NEGOCIACION PRECIO)

  # each registro is a row on your spreadsheet
  @registros.each do |registro|
    sheet.add_row [registro.producto.marca, registro.nivel, registro.negociacion, registro.precio]
  end
end
